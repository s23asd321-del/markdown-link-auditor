# Examples

This directory contains two fixture projects:

- [good-docs](good-docs/README.md) should pass.
- [bad-docs](bad-docs/README.md) intentionally contains fake/demo issues and should fail.

The repository root uses `.markdown-link-auditor-ignore` so the intentionally broken example does not fail the root self-check.
