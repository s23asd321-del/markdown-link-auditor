# TODO

- Keep the first version focused on local Markdown files.
- Improve parser coverage only when tests describe the expected behavior.
- Add CommonMark-inspired fixtures for nested parentheses, escaped characters, and complex reference links.
- Add anchor slug fixtures for Chinese, Japanese, emoji, punctuation, and duplicate headings.
- Explore strict/simple parser modes before changing default parsing behavior.
- Consider optional configuration after the initial public release.
- Explore optional no-default-network external link checking with timeout, cache, retry, and domain allowlist controls.
- Evaluate SARIF, JUnit XML, and GitHub annotations for CI output.
- Add more real-world fixture cases for unusual but common Markdown patterns.
- Keep report formats stable before documenting them as machine contracts.
